<ul class="{% if gap != blank %} grid grid-cols-4 {{ gap }} {% else %} flex justify-between {% endif %} h-max w-max" role="list">
    {%- if settings.social_twitter_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_twitter_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'twitter' -%}
                <span class="sr-only">{{ 'general.social.links.twitter' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_facebook_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_facebook_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'facebook' -%}
                <span class="sr-only">{{ 'general.social.links.facebook' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_pinterest_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_pinterest_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'pinterest' -%}
                <span class="sr-only">{{ 'general.social.links.pinterest' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_instagram_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_instagram_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'instagram' -%}
                <span class="sr-only">{{ 'general.social.links.instagram' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_tiktok_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_tiktok_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'tiktok' -%}
                <span class="sr-only">{{ 'general.social.links.tiktok' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_tumblr_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_tumblr_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'tumblr' -%}
                <span class="sr-only">{{ 'general.social.links.tumblr' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_snapchat_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_snapchat_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'snapchat' -%}
                <span class="sr-only">{{ 'general.social.links.snapchat' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_youtube_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_youtube_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'youtube' -%}
                <span class="sr-only">{{ 'general.social.links.youtube' | t }}</span>
            </a>
        </li>
    {%- endif -%}
    {%- if settings.social_vimeo_link != blank -%}
        <li class="flex items-center justify-center mx-1">
            <a href="{{ settings.social_vimeo_link }}" class="block transition-base opacity-60 hover:opacity-100 {{ size | default: 'w-8 h-5' }}" aria-describedby="a11y-external-message">
                {%- render 'icons' icon: 'vimeo' -%}
                <span class="sr-only">{{ 'general.social.links.vimeo' | t }}</span>
            </a>
        </li>
    {%- endif -%}
</ul>
